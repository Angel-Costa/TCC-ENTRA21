
@{
    ViewBag.Title = "Index";
}

<h4 class="text-center">Lista de Clientes</h4>
<div class="row">
    <div class="col-11 offset-1 mb-4">
        <a class="btn btn-primary float-right"
           href="@Url.Action("Cliente")">
            <i class="fa fa-plus"></i> Novo Cliente</a>
    </div>
</div>

<div class="row">
    <div class="col-10 offset-1">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Estado</th>
                    <th>Cidade</th>
                    <th>Cep</th>
                    <th>Bairro</th>
                    <th>Número</th>
                    <th>Rua</th>
                    <th>Complemento</th>
                    <th>Login</th>
                    <th>Senha</th>
                </tr>
            </thead>
            <tbody>

                @foreach(Model.Cliente cliente in ViewBag.Clientes)
                {
                    <tr>
                        <td>@cliente.Id</td>
                        <td>@cliente.Nome</td>
                        <td>
                            <a href="@Url.Action("Editar", new { Id = cliente.Id})"
                               class="btn btn-primary">
                                <i class="fa-fast-backward fa-pen"></i> Editar
                            </a>
                            <a href="@Url.Action("Apagar", new { Id = cliente.Id})" class="btn btn-danger">
                                <i class="fa-fast-backward fa-trash"></i> Apagar
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

